package main

import (
	"fmt"
)

func main() {
	tokens := []string{"4", "13", "5", "/", "+"}
	fmt.Println(evalRPN(tokens)) // Output: 9
}
func generateParenthesis(n int) []string {
	m := map[rune]rune{'(': ')'}
	s := []string{}
	for i := 0; i < n; i++ {
		
	}
}
